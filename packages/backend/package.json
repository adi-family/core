{
  "name": "@adi-simple/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch server-native.ts",
    "start": "bun --env-file=../../.env server-native.ts",
    "build": "bun build server-native.ts --outdir dist --target bun",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "postgres": "^3.4.7",
    "zod": "^4.1.12",
    "@adi-simple/db": "*",
    "@adi-simple/utils": "*",
    "@adi-simple/shared": "*",
    "@adi-simple/types": "*",
    "@adi-simple/worker": "*",
    "@adi/queue": "*",
    "@adi/api-contracts": "*",
    "@adi-family/http": "^1.0.3",
    "@adi-family/http-express": "^1.0.3",
    "@adi-family/http-native": "^1.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.7.1",
    "typescript": "~5.9.3"
  }
}
