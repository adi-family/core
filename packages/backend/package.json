{
  "name": "@adi-simple/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch server-native.ts",
    "start": "bun --env-file=../../.env server-native.ts",
    "build": "bun build server-native.ts --outdir dist --target bun",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "postgres": "^3.4.7",
    "zod": "^4.1.12",
    "@adi-simple/db": "workspace:*",
    "@adi-simple/utils": "workspace:*",
    "@adi-simple/shared": "workspace:*",
    "@adi-simple/types": "workspace:*",
    "@adi-simple/worker": "workspace:*",
    "@adi/queue": "workspace:*",
    "@adi/api-contracts": "workspace:*",
    "@adi-family/http": "^1.0.0",
    "@adi-family/http-express": "^1.0.0",
    "@adi-family/http-native": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.7.1",
    "typescript": "~5.9.3"
  }
}
