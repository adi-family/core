# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5436/postgres?sslmode=disable

# RabbitMQ
RABBITMQ_URL=amqp://rabbit:rabbit@localhost:5672

# Port Configuration
CLIENT_PORT=5173
SERVER_PORT=5174
LANDING_PORT=3000
BACKEND_URL=http://localhost:5174

# Landing Page
NEXT_PUBLIC_APP_URL=http://localhost:5173

# Scheduler Configuration (optional - for periodic task source polling)
# ENABLE_SCHEDULER=true
# SCHEDULER_INTERVAL_MS=600000
# DEFAULT_RUNNER=claude

# Worker Configuration (DEPRECATED - only needed for GitLab CI pipeline scripts)
# The worker polling service (worker/index.ts) has been replaced by backend orchestrator
# These vars are only used by GitLab CI worker scripts, not by the worker polling service
APPS_DIR=.apps

# Pipeline Monitoring (optional)
PIPELINE_STATUS_TIMEOUT_MINUTES=30
PIPELINE_POLL_INTERVAL_MS=600000

# Execution Mode (optional)
# USE_PIPELINE_EXECUTION=true

# Testing & Debugging (optional)
# MOCK_MODE=true  # Enable mock mode to skip real AI API calls in worker pipelines

# Agent API Keys (for local execution or GitLab CI/CD variables)
# ANTHROPIC_API_KEY=your-key-here
# OPENAI_API_KEY=your-key-here
# GOOGLE_API_KEY=your-key-here

# GitLab Configuration (for worker repository management)
# GITLAB_HOST=https://gitlab.the-ihor.com
# GITLAB_TOKEN=your-gitlab-token
# GITLAB_USER=your-gitlab-username

# Security (required for production)
# ENCRYPTION_KEY=your-encryption-key-min-32-chars
# API_TOKEN=your-api-token-for-pipeline-auth

# Clerk Authentication (required for user access control)
# CLERK_SECRET_KEY=sk_test_...
# CLERK_PUBLISHABLE_KEY=pk_test_...

# Webhook Security (optional)
# GITLAB_WEBHOOK_SECRET=your-gitlab-webhook-secret
# JIRA_WEBHOOK_SECRET=your-jira-webhook-secret
# GITHUB_WEBHOOK_SECRET=your-github-webhook-secret
